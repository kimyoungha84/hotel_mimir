<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.sist.payment.PaymentMapper">

	<select id="selectAllPayment" resultType="kr.co.sist.payment.PaymentDomain">
		SELECT faq_num, faq_title, faq_content, faq_date
		FROM faq
		ORDER BY faq_date DESC
	</select>
	
	<select id="selectOnePayment" parameterType="int" resultType="kr.co.sist.payment.PaymentDomain">
	select faq_num, faq_title, faq_content
	from faq
	where faq_num = #{faq_num}

	</select>

	<select id="selectPaymentSeq" resultType="int">
    SELECT SEQ_PAYMENT.NEXTVAL FROM DUAL
	</select>

	<insert id="insertPayment" parameterType="kr.co.sist.resvroom.ReservationDTO">
		INSERT INTO payment (PAYMENT_ID, PAYMENT_PRICE,
		 PAYMENT_TYPE, PAYMENT_STATUS)
		VALUES (#{paymentId}, #{paymentPrice}, 
		#{paymentType}, #{paymentStatus})
	</insert>
	
  <insert id="insertPayment2" parameterType="kr.co.sist.diningresv.DiningResvDTO">
    INSERT INTO payment (PAYMENT_ID, PAYMENT_PRICE,
     PAYMENT_TYPE, PAYMENT_STATUS, PAYMENT_TIME)
    VALUES (#{paymentId}, #{paymentPrice}, 
    #{paymentType}, #{paymentStatus}, #{paymentTime})
  </insert>
	
	<update id="updatePayment" parameterType="kr.co.sist.payment.PaymentDTO">
	  UPDATE faq
	  SET faq_title = #{faq_title},
	      faq_content = #{faq_content}
	  WHERE faq_num = #{faq_num}
	</update>
	
</mapper>
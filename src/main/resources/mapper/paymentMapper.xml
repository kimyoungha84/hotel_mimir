<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.sist.payment.PaymentMapper">

	<select id="selectPaymentSeq" resultType="int">
    SELECT SEQ_PAYMENT.NEXTVAL FROM DUAL
	</select>

	<insert id="insertPayment" parameterType="kr.co.sist.resvroom.ReservationDTO">
		INSERT INTO payment (PAYMENT_ID, PAYMENT_PRICE,
		 PAYMENT_TYPE, PAYMENT_STATUS)
		VALUES (#{paymentId}, #{paymentPrice}, 
		#{paymentType}, #{paymentStatus})
	</insert>
	
  <insert id="insertPayment2" parameterType="kr.co.sist.diningresv.DiningResvDTO">
    INSERT INTO payment (PAYMENT_ID, PAYMENT_PRICE,
     PAYMENT_TYPE, PAYMENT_STATUS, PAYMENT_TIME)
    VALUES (#{paymentId}, #{paymentPrice}, 
    #{paymentType}, #{paymentStatus}, #{paymentTime, jdbcType=TIMESTAMP})
  </insert>
	
</mapper>
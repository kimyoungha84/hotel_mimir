<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.sist.resvroom.ReservationMapper">

  <select id="selectAllResv" resultType="reservationDTO">
 SELECT 
    r.RESV_ID          AS resvId,
    r.ROOM_ID          AS roomId,
    rt.TYPE_NAME       AS typeName,
    r.USER_NUM         AS userNum,
    u.USER_NAME        AS userName,
    u.TEL              AS userTel,
    u.EMAIL_ID         AS userEmail,
    r.NON_MEM_ID       AS nonMemId,
    nm.NON_MEM_NAME    AS nonMemName,
    nm.NON_MEM_TEL     AS nonMemTel,
    r.PAYMENT_ID       AS paymentId,
    r.CHECKIN_DATE     AS checkinDate,
    r.CHECKOUT_DATE    AS checkoutDate,
    r.NUM_GUESTS       AS numGuests,
    r.STATUS           AS status,
    r.RESV_REG_DATE    AS resvRegDate,
    r.REQUEST_MSG      AS requestMsg,
    p.PAYMENT_PRICE    AS paymentPrice
  FROM RESERVATION r
  LEFT JOIN ROOM rm ON r.ROOM_ID = rm.ROOM_ID
  LEFT JOIN ROOM_TYPE rt ON rm.TYPE_ID = rt.TYPE_ID
  LEFT JOIN MIMIR_USER u ON r.USER_NUM = u.USER_NUM
  LEFT JOIN NON_MEMBER nm ON r.NON_MEM_ID = nm.NON_MEM_ID
  LEFT JOIN PAYMENT p ON r.PAYMENT_ID = p.PAYMENT_ID
  </select>

	<select id="selectAllReservation" resultType="kr.co.sist.resvroom.ReservationDomain">
		SELECT faq_num, faq_title, faq_content, faq_date
		FROM faq
		ORDER BY faq_date DESC
	</select>
	
	<select id="selectOneReservation" parameterType="int" resultType="kr.co.sist.resvroom.ReservationDomain">
	select faq_num, faq_title, faq_content
	from faq
	where faq_num = #{faq_num}

	</select>
	
	<insert id="insertReservation" parameterType="kr.co.sist.resvroom.ReservationDTO">
		INSERT INTO faq (faq_num, faq_title, faq_content, faq_date)
		VALUES (faq_seq.NEXTVAL, #{faq_title}, #{faq_content}, #{faq_date})
	</insert>

	<update id="updateReservation" parameterType="kr.co.sist.resvroom.ReservationDTO">
	  UPDATE faq
	  SET faq_title = #{faq_title},
	      faq_content = #{faq_content}
	  WHERE faq_num = #{faq_num}
	</update>
	
</mapper>
// 다이닝 메인 페이지 JavaScript

// jQuery 문서 준비 이벤트
$(function() {
  // 페이지 로드 시 초기화 작업
  initDiningMain();
});

// 다이닝 메인 페이지 초기화
function initDiningMain() {
  // 카드 호버 효과
  initCardHoverEffects();
  
  // 필터링 기능 초기화 (필요시)
  initFiltering();
}

// 카드 호버 효과 초기화
function initCardHoverEffects() {
  // 카드에 마우스 오버 시 이미지 확대 효과는 CSS에서 처리됨
  // 추가적인 JavaScript 효과가 필요한 경우 여기에 구현
}

// 필터링 기능 초기화
function initFiltering() {
  // 필터링 관련 기능이 필요한 경우 여기에 구현
  // 현재는 fragment로 처리되고 있음
} 